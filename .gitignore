# dependencies
node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts

# turbo
.turbo/

# build output
dist/
build/

# env
.env
.env*.local
.dev.vars

# misc
.DS_Store
*.log
coverage/

# Gas City orchestration — runtime state + bundled system packs, all regenerable
# via `gc`. The durable, version-controlled city config lives in ops/gascity/.
.gc/
.beads/

# agent git worktrees (Scribe/Porter implement dirs — local only)
.worktrees/

// Parvus Gather (RFC-005) — the parish coordination module: groups + RBAC + the Requests spine.
// One barrel per submodule (the uniform packages/core convention, po-a56); sibling tiers add
// their own re-exports here (RBAC engine, Requests pillar) alongside the Groups primitive.
export * from "./groups";

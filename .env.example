# Local development environment (copy to .env). Docker Compose defaults below.

# Superuser connection — used ONLY by migrations and the seed.
MIGRATION_DATABASE_URL=postgres://postgres:postgres@localhost:5432/parvaordo

# App-role connection — RLS-enforced. Used by the app runtime and integration tests.
# In DEPLOYED envs, point this at Neon's POOLED (-pooler) endpoint so PgBouncer
# transaction-mode pooling bounds Postgres-side backends (RFC-002 §2A). Local dev uses
# the direct endpoint below.
DATABASE_URL=postgres://parvaordo_app:app_local_pw@localhost:5432/parvaordo

# Optional connection-budget knobs (RFC-002 §2C/§2D); safe defaults if unset. pool.max ×
# wrangler max_instances must stay under the endpoint's connection limit. The timeouts let
# a saturated/overloaded container fail fast (503) instead of hanging.
# DB_POOL_MAX=10
# DB_CONNECTION_TIMEOUT_MS=5000
# DB_STATEMENT_TIMEOUT_MS=30000

# Base domain for parish subdomains, per environment. A request to
# <slug>.<PARISH_BASE_DOMAIN> resolves to that parish (migration 0010).
#   local: localhost   dev: dev.parvusordo.com   stage: stage.parvusordo.com   prod: parvusordo.com
PARISH_BASE_DOMAIN=localhost

# Dedicated signing secret for app-issued /api/v1 bearer tokens (Parvus Studio iOS).
# Optional: if unset, core/auth/api-token falls back to WORKOS_COOKIE_PASSWORD. Set it
# to a long random value (kept distinct from the cookie password) to separate the two
# trust domains; changing it invalidates outstanding app tokens, so clients re-login.
# API_TOKEN_SECRET=

# Test Catalog

> Auto-generated by `pnpm test:catalog` from the test files. Do not edit by hand —
> the `describe`/`it`/`test` titles in the specs are the source of truth.

**122 test cases** across 22 files.

### Unit — 50 cases

Pure logic, no DB (Vitest). `pnpm test`

#### `packages/core/src/branding/branding.test.ts` — 4 cases

- **resolveBrand**
  - [ ] returns the default Parvus Ordo brand (stub cascade)
  - [ ] tolerates a null hostname
- **DEFAULT_BRAND**
  - [ ] carries the Parvus Ordo identity
  - [ ] defines the full six-color palette as hex

#### `packages/core/src/media/media.test.ts` — 8 cases

- **mapBunnyStatus**
  - [ ] maps finished → ready with full progress + duration
  - [ ] maps in-flight codes → processing with encode progress
  - [ ] maps just-uploaded → uploading
  - [ ] maps error/upload-failed → failed
- **clipTranscript**
  - [ ] keeps only words overlapping the clip window
  - [ ] treats null end as open-ended
- **formatTranscript**
  - [ ] falls back to verbatim text when there are no word timings
  - [ ] groups word-timed transcripts into [m:ss] ~10s blocks

#### `packages/core/src/media/transcription.test.ts` — 5 cases

- **splitWav**
  - [ ] returns a single chunk when under the cap
  - [ ] splits on frame boundaries when over the cap, each a valid WAV
  - [ ] uses a sane cap by default
- **transcribeChunked**
  - [ ] does not offset a single-chunk transcript
  - [ ] offsets each chunk's word timestamps by cumulative duration

#### `packages/core/src/onboarding/roles.test.ts` — 4 cases

- **canInviteRole**
  - [ ] admin and super_admin can invite any invitable role
  - [ ] a catechist can invite students/catechists/members but NOT an admin
  - [ ] learners/members and null roles cannot invite anyone
  - [ ] super_admin is never invitable, even by an admin

#### `packages/core/src/platform/hostname.test.ts` — 10 cases

- **resolveParishRef**
  - **base = parvusordo.com (prod)**
    - [ ] the bare base domain is the apex
    - [ ] www/app map to the apex, not a parish
    - [ ] a single leftmost label is the parish slug
    - [ ] a host not under the base domain is a custom domain
    - [ ] deeper sub-subdomains are not parish slugs (→ apex)
  - **environment portability — same slug, every base domain**
    - [ ] resolves holy-spirit by slug in local/dev/stage/prod
  - **normalization**
    - [ ] strips the port
    - [ ] is case-insensitive
    - [ ] null/empty host is the apex
    - [ ] bare localhost is the apex

#### `packages/core/src/platform/identity.test.ts` — 7 cases

- **pickActiveMembership**
  - [ ] returns null when there are no memberships
  - [ ] returns the sole membership automatically
  - [ ] an explicit parishId (chooser pick / cookie) wins
  - [ ] falls back to the host-resolved parish
  - [ ] an explicit parishId beats the host-resolved parish
  - [ ] is ambiguous (→ null, show chooser) with 2+ memberships and no hint
  - [ ] ignores a parishId the user doesn't hold and uses the sole membership

#### `packages/shared/src/clip-enforcement.test.ts` — 9 cases

- **clipTimeUpdate (per-tick)**
  - [ ] does NOT clamp to the window start while buffering toward a non-zero start (regression)
  - [ ] clamps no-skip-ahead beyond maxReached + 2s when unwatched
  - [ ] allows movement within the maxReached + 2s grace
  - [ ] does not enforce no-skip once watched
  - [ ] flags atEnd at/after the window end
  - [ ] never flags atEnd for an open-ended window
- **clipSeek (manual seek)**
  - [ ] clamps a seek before the window start up to the start
  - [ ] clamps a forward seek beyond maxReached + 2s when unwatched
  - [ ] allows free seeking once watched

#### `packages/shared/src/duration.test.ts` — 3 cases

- **estimateItemsDuration**
  - [ ] sums video clip length, reading words/200wpm, and 60s per question
  - [ ] ignores a video with no end set (unknown length)
  - [ ] strips HTML before counting reading words

---

### Integration — 51 cases

Real Postgres + RLS (Vitest). `pnpm test:int`

#### `packages/core/src/media/assets.int.test.ts` — 6 cases

- **asset lifecycle**
  - [ ] creates a video asset pending transcription; reads it back
  - [ ] an image asset does not request transcription
  - [ ] advances transcode status and attaches playback metadata
  - [ ] stores a transcript (text + word timings) and marks it completed
  - [ ] filters by kind
  - [ ] RLS hides one parish's asset from another (different diocese)

#### `packages/core/src/media/clips.int.test.ts` — 4 cases

- **clips (stub processor)**
  - [ ] requestClip creates a ready clip asset linked to its source; removeClip deletes it
  - [ ] removeClip refuses to delete a non-clip (source) asset
  - [ ] deleting a source cascades to its clips
- **video watch progress**
  - [ ] persists the furthest point (grows only) and sticky completion

#### `packages/core/src/ocia/feedback.int.test.ts` — 1 case

- **student questions + feedback**
  - [ ] captures questions/feedback and lists them per lesson with the student's name

#### `packages/core/src/ocia/lessons.int.test.ts` — 15 cases

- **three-tier visibility (published, live versions)**
  - [ ] Holy Spirit sees global + its diocese + its own, not St. Monica's
  - [ ] a parish in another diocese sees global only
  - [ ] getLessonDetail returns the live version's ordered items
  - [ ] hides another parish's lesson (RLS null)
- **versioning lifecycle**
  - [ ] a new lesson is a draft — invisible to students until published
  - [ ] editing a published lesson makes a draft; students keep seeing the live version; publish/rollback/unpublish
  - [ ] enforces a single draft per lesson (partial unique index)
- **fork**
  - [ ] forks a global lesson into a parish-owned published copy; original untouched; isolated
  - [ ] a parish cannot edit a global lesson (editable=false)
- **delete**
  - [ ] deletes a whole lesson (cascade)
  - [ ] discards a draft, but refuses to delete the only version or the live version
- **manage sort + filter**
  - [ ] filters by scope
  - [ ] filters by status
  - [ ] sorts by title
  - [ ] sorts by recent (timestamps are ISO strings, not Date objects)

#### `packages/core/src/ocia/progress.int.test.ts` — 3 cases

- **answers + progress (integration)**
  - [ ] upserts an answer (submit then edit)
  - [ ] marks items complete (idempotent) and reports completion
  - [ ] does not leak one parish's answers/progress to another (RLS)

#### `packages/core/src/onboarding/applicants.int.test.ts` — 6 cases

- **ocia applicants (integration)**
  - [ ] creates a pending applicant when applications are open, and lists it
  - [ ] rejects submissions when applications are closed
  - [ ] throttles a rapid resubmit from the same email
  - [ ] converts an applicant to an invited catechumen/candidate (membership created)
  - [ ] isolates applicants by parish (RLS): an HS applicant is invisible under St. Monica
  - [ ] dismisses a pending applicant

#### `packages/core/src/onboarding/invite.int.test.ts` — 5 cases

- **inviteMember (integration)**
  - [ ] invites a new person: creates the user + membership; no email without a WorkOS key
  - [ ] dedupes the same (email, parish, role) → conflict
  - [ ] a catechist may NOT mint an admin → forbidden
  - [ ] a catechist may invite a catechumen/candidate
  - [ ] rejects a malformed email

#### `packages/core/src/platform/hostname.int.test.ts` — 4 cases

- **resolveParishIdForHost (resolve_parish_id, integration)**
  - [ ] resolves a parish subdomain to its id by slug
  - [ ] is case-insensitive and ignores the port
  - [ ] returns null for the apex (no parish)
  - [ ] returns null for an unknown slug / custom domain

#### `packages/core/src/platform/login.int.test.ts` — 4 cases

- **lookupAppUser (login_lookup, integration)**
  - [ ] resolves a seeded admin to role + parish
  - [ ] is case-insensitive on email
  - [ ] resolves the ministry-scoped catechist to catechist + parish
  - [ ] returns null for an unknown email

#### `packages/core/src/platform/rls.int.test.ts` — 3 cases

- **RLS tenant isolation (integration)**
  - [ ] returns no parish rows when no tenant is set
  - [ ] returns exactly the active parish row
  - [ ] never leaks one parish's ministries to another

---

### End-to-end — 21 cases

Playwright, desktop + mobile. `pnpm test:e2e`

#### `apps/web/e2e/apply.spec.ts` — 3 cases

- [ ] public applicant applies on the parish subdomain, admin converts to student
- [ ] admin invites a member directly by email
- [ ] the public apply form is closed when applications are disabled

#### `apps/web/e2e/auth.spec.ts` — 11 cases

- [ ] unauthenticated / redirects to /login
- [ ] /login shows a sign-in affordance
- [ ] /sign-in redirects to the WorkOS hosted screen
- [ ] dev bypass: admin dashboard shows parish, ministries, and the OCIA module
- [ ] dev bypass: an OCIA-only learner lands in OCIA Home (no parish dashboard)
- [ ] dev bypass: lesson is a one-item-at-a-time wizard with progress + gating
- [ ] dev bypass: catechist builds, publishes, re-edits, then cleans up a lesson
- [ ] dev bypass: catechist forks a diocese lesson, then cleans it up
- [ ] dev bypass: manage list filters by type
- [ ] super-admin can impersonate a role and exit
- [ ] dev bypass: cannot skip ahead past the first incomplete item

#### `apps/web/e2e/media.spec.ts` — 5 cases

- [ ] media library lists the seeded video as ready + transcribed
- [ ] learner reaches the video step: seek-enforcing player + synced transcript render
- [ ] learner completes a lesson → asks a question + feedback → reviews answers; catechist sees them
- [ ] teacher preview starts at the beginning, ungated, with a jump-to dropdown
- [ ] catechist adds a video item and the iMovie-style trimmer mounts

#### `apps/web/e2e/multi-parish.spec.ts` — 2 cases

- [ ] multi-parish user chooses a parish, then switches (role changes per parish)
- [ ] single-parish user never sees the chooser or switcher
